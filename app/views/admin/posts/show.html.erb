
<div class="row">
    <div class="col-sm-12 text-center">
        <h3><%= @post.title %></h3>
        <hr>
        <p class="category"><%= image_tag(@post.image.url(:medium)) %></p>
        <p class="text-left"><%= simple_format(@post.description, {}, :sanitize => false) %></p>
        
    </div>
</div>
